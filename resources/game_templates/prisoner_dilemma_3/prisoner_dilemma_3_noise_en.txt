You are {currentPlayerName} and your opponents are {opponent1} and {opponent2}.
{intro}: [You are {personality}.]
{opponentIntro}: [Your opponents: {opponent1} ({opponentPersonality1} - {opponentPersonalityProbability1}% prob), {opponent2} ({opponentPersonality2} - {opponentPersonalityProbability2}% prob).]
{noiseIntro}: [IMPORTANT: Your opponents have noise rates. {opponent1}: {opponentNoiseRate1}%, {opponent2}: {opponentNoiseRate2}%. This means their actual action may differ from their intended action.]
{gameLength}: [There are {nRounds} rounds to decide.] 
The current round is number {currentRound}.

Every round each of you has the following choices: '{strategy1}' and '{strategy2}'.

Payoffs (lower is better penalty):
- If YOU choose {strategy1} (Defect):
    - If BOTH opponents choose {strategy1}: You get {weight_P}.
    - If ONE opponent chooses {strategy1} and ONE chooses {strategy2}: You get {weight_T1}.
    - If BOTH opponents choose {strategy2}: You get {weight_T2}.

- If YOU choose {strategy2} (Cooperate):
    - If BOTH opponents choose {strategy1}: You get {weight_S0}.
    - If ONE opponent chooses {strategy1} and ONE chooses {strategy2}: You get {weight_S1}.
    - If BOTH opponents choose {strategy2}: You get {weight_R}.

Your goal is to minimize your penalty.
History: {history}.
{historyNote}: [Note: The actions shown in history are FINAL actions after noise was applied.]
Choose between {strategy1} and {strategy2}.
Output ONLY a valid JSON object with the following format: 
{{
  "action": "your choice ({strategy1} or {strategy2})", 
  "beliefs": {{
    "opponent1_coop_prob": 50,  // probability (0-100) {opponent1} will choose '{strategy2}'
    "opponent2_coop_prob": 80,  // probability (0-100) {opponent2} will choose '{strategy2}'
    "opponent1_noise_suspicion": 30,  // estimate (0-100): was {opponent1}'s LAST action noise (100) or strategic (0)? Noise rate: {opponentNoiseRate1}%
    "opponent2_noise_suspicion": 10   // estimate (0-100): was {opponent2}'s LAST action noise (100) or strategic (0)? Noise rate: {opponentNoiseRate2}%
  }},
  "reason": "brief explanation (1-2 sentences)"
}}
