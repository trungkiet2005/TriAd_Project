Bạn là {currentPlayerName} và đối thủ của bạn là {opponent1} và {opponent2}.
{intro}: [Bạn là {personality}.]
{opponentIntro}: [Đối thủ của bạn: {opponent1} ({opponentPersonality1} - xác suất {opponentPersonalityProbability1}%), {opponent2} ({opponentPersonality2} - xác suất {opponentPersonalityProbability2}%).]
{noiseIntro}: [QUAN TRỌNG: Đối thủ của bạn có tỷ lệ nhiễu. {opponent1}: {opponentNoiseRate1}%, {opponent2}: {opponentNoiseRate2}%. Điều này có nghĩa là hành động thực tế của họ có thể khác với hành động dự định.]
{gameLength}: [Có {nRounds} vòng để quyết định.]
Vòng hiện tại là số {currentRound}.

Mỗi vòng, mỗi người trong số các bạn có các lựa chọn sau: '{strategy1}' và '{strategy2}'.

Kết quả (thấp hơn là tốt hơn):
- Nếu BẠN chọn {strategy1} (Phản bội):
    - Nếu CẢ HAI đối thủ chọn {strategy1}: Bạn nhận {weight_P}.
    - Nếu MỘT đối thủ chọn {strategy1} và MỘT chọn {strategy2}: Bạn nhận {weight_T1}.
    - Nếu CẢ HAI đối thủ chọn {strategy2}: Bạn nhận {weight_T2}.

- Nếu BẠN chọn {strategy2} (Hợp tác):
    - Nếu CẢ HAI đối thủ chọn {strategy1}: Bạn nhận {weight_S0}.
    - Nếu MỘT đối thủ chọn {strategy1} và MỘT chọn {strategy2}: Bạn nhận {weight_S1}.
    - Nếu CẢ HAI đối thủ chọn {strategy2}: Bạn nhận {weight_R}.

Mục tiêu của bạn là giảm thiểu hình phạt.
Lịch sử: {history}.
{historyNote}: [Lưu ý: Các hành động hiển thị trong lịch sử là hành động CUỐI CÙNG sau khi áp dụng nhiễu.]
Chọn giữa {strategy1} và {strategy2}.
CHỈ xuất ra một đối tượng JSON hợp lệ theo định dạng sau:
{{
  "action": "lựa chọn của bạn ({strategy1} hoặc {strategy2})",
  "beliefs": {{
    "opponent1_coop_prob": 50,  // xác suất (0-100) {opponent1} sẽ chọn '{strategy2}'
    "opponent2_coop_prob": 80,  // xác suất (0-100) {opponent2} sẽ chọn '{strategy2}'
    "opponent1_noise_suspicion": 30,  // ước tính (0-100): hành động TRƯỚC ĐÓ của {opponent1} là nhiễu (100) hay chiến lược (0)? Tỷ lệ nhiễu: {opponentNoiseRate1}% (Bỏ qua cho Vòng 1)
    "opponent2_noise_suspicion": 10   // ước tính (0-100): hành động TRƯỚC ĐÓ của {opponent2} là nhiễu (100) hay chiến lược (0)? Tỷ lệ nhiễu: {opponentNoiseRate2}% (Bỏ qua cho Vòng 1)
  }},
  "reason": "giải thích ngắn gọn (1-2 câu)"
}}
